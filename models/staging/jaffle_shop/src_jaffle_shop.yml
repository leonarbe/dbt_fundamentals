version: 2

sources:
  - name: JAFFLE_SHOP
    database: RAW

    schema: JAFFLE_SHOP
    tables:
      - name: CUSTOMERS
      - name: ORDERS
        #load_at_fields: etl_loaded_at
        #freshness:
        #  warn_after: {count: 12, period: hour}
        #  error_after: {count: 24, period: hour}